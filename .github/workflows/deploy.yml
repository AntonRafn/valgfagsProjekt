name: Publish Website

on:
  push:
    branches:
      - main


jobs:
  web-deploy:
    name: 🚀 Deploy Website Every Commit
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get Latest Code
      uses: actions/checkout@v3
    
    - name: 📂 Sync Files
      uses: SamKirkland/web-deploy@v1
      with:
        target-server: ssh.ci1oaw5p7.service.one
        remote-user: ci1oaw5p7_ssh
        private-ssh-key: ${{ secrets.SSH_KEY }}
        source-path: ./wp-content/themes/valgfagsProjekt/
        destination-path: webroots/by-route/valgfagtest.antonshjemmeside.dk_/wp-content/themes/valgfagsProjekt